NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Promote-page payment details (server-only, all optional).
# Set whichever methods you offer; the page shows only the ones fully configured,
# and falls back to a "contact us" notice if none are. Bank transfer needs all
# three of NAME / ACCOUNT_NAME / ACCOUNT_NUMBER before it renders.
PROMO_BANK_NAME=
PROMO_ACCOUNT_NAME=
PROMO_ACCOUNT_NUMBER=
PROMO_PRICE_LKR=
PROMO_PAYHERE_URL=
PROMO_CONTACT_EMAIL=
